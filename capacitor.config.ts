import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.effner.web',
  appName: 'EffnerApp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
