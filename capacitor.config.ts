import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'effnerapp@web',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
